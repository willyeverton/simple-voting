simple_voting.settings:
  type: config_object
  label: 'Simple Voting settings'
  mapping:
    voting_enabled:
      type: boolean
      label: 'Voting enabled'

# Retained while legacy question configuration remains available for migration
# verification and rollback.
simple_voting.question.*:
  type: config_entity
  label: 'Legacy voting question'
  mapping:
    id:
      type: string
      label: 'Machine name'
    uuid:
      type: uuid
      label: 'UUID'
    title:
      type: string
      label: 'Title'
    status:
      type: boolean
      label: 'Open for voting'
    show_results:
      type: boolean
      label: 'Show results'
    created:
      type: integer
      label: 'Created timestamp'
    changed:
      type: integer
      label: 'Changed timestamp'
